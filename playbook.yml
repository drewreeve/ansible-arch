---
- hosts: localhost
  become: yes
  roles:
    - { role: base, tags: ['base'] }
    - { role: editors }
    - { role: microcode }
    - { role: x }
  vars_prompt:
    - name: user_password
      prompt: "Enter desired user password (only takes effect on creation)"
      unsafe: yes
      private: yes
